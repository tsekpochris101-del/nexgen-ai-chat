PORT=4000
HOST=0.0.0.0
SESSION_SECRET=replace-with-a-long-random-value
GROQ_API_KEY=
EXA_API_KEY=
ADMIN_EMAIL=
GROQ_MODEL=openai/gpt-oss-120b
# Leave empty when the frontend and API share one public origin.
# For a separate frontend, use a comma-separated list of allowed origins.
APP_ORIGIN=